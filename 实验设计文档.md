# 地基判决实验设计文档

> 版本 v1.0（预注册草案）。两个实验均为 1–2 周内完成的 go/no-go 判决实验。
> 本文档只含协议、伪代码与配置，不含实现代码。

---

## 第 0 部分：共同工程规范（两实验共用）

**硬件与精度**
- 目标卡：单卡 48GB（A6000 / L40S 级）。FLUX.1-dev bf16 权重约 24GB，可加载；若需更大 batch，用 bitsandbytes nf4 / fp8 量化 transformer 至 ~12–16GB（VAE 与 T5/CLIP 保持 bf16）。
- **量化有效性前置校验**：固定 10 个 (t, ω) 单元 × 8 种子，比较 bf16 与量化版的终端扰动幅度，要求相对偏差 < 5%，否则主实验一律用 bf16。
- 所有 FFT/IFFT 在 fp32 下执行（bf16 直接做频域运算会引入与扰动同量级的数值噪声，属致命混杂）。
- VAE decode 固定用同一 decoder、同一精度；latent 按 FLUX 约定 scaling=0.3611、shift=0.1159。

**配对设计（方差控制的核心）**
- 所有"加扰 vs 不加扰"对比必须使用**同一初始噪声种子、同一 prompt、同一采样轨迹缓存**。终端差 Δ = x̂₀^pert − x̂₀^clean 是配对差，不是跨样本差。
- 种子池固定为 seed ∈ {1000…1099}，预注册，不许按结果挑选。

**预注册**
- 所有扫描网格、样本量、判决阈值在跑数前冻结到 `protocol.yaml`；任何事后改动单独标记为"探索性分析"。

---

# 实验 1：FLUX 速度场频域灵敏度定律验证

## 1.1 目标与假设

**主假设 H1**：在高斯谱参考模型下，于时刻 t 对 RF 速度场的频率 ω 分量施加脉冲扰动 δ，终端图像扰动幅度满足

A(t, ω) ∝ 1/C(t, ω)，C(t, ω) = √((1−t)² + t²/S(ω))

- 灵敏度曲线关于 t 呈 ∩ 形，峰值 t_c(ω) = S(ω)/(1+S(ω))；
- 低频（S 大）峰值靠近噪声端 t→1，高频（S 小）峰值靠近数据端 t→0；
- 高频扰动在噪声端被压制约 |ω|^(−β/2) 倍（设 S(ω) ∝ |ω|^(−β)）。

**约定复核**：t=0 数据端，t=1 噪声端，采样从 t=1 积分到 t=0。对 t 求导验证：(1−t)² + t²/S 极小点即 t_c = S/(1+S)。✓

**备择假设（预先登记，任何结果都有结论）**
- H2a（定性序成立、定量失效）：∩ 形存在且 t̂_c 随 ω 单调左移，但 |t̂_c − t_c| 超差 → 高斯谱标定错了，换 S 的估计量重拟合。
- H2b（单调型）：A 关于 t 单调 → 灵敏度核不含维纳收缩结构，退化为"恒定核"或"score 主导"模型。
- H2c（非线性 regime）：响应不随 δ 线性缩放 → 脉冲响应理论前提（小扰动线性化）不成立，需报告线性化失效的临界 δ。

## 1.2 材料与配置

| 项 | 配置 |
|---|---|
| 主模型 | FLUX.1-dev（12B，bf16），Euler 离散化，T=28 步，embedded guidance=3.5 |
| 次模型 | FLUX.1-schnell，T=4 步，guidance=0（蒸馏无 CFG） |
| 对照 | SD2.1（v-prediction U-Net，860M），DDIM η=0，T=50 步，CFG=7.5，负 prompt 为空串 |
| 分辨率 | 512×512（latent：FLUX 16ch×64×64；SD2.1 4ch×64×64）。1024² 仅做 5 单元 × 8 种子的抽查复核 |
| Prompt 池 | 32 条中性 prompt（"a photo of …"类，预注册固定），每种子轮换 |
| 时间轴 | **必须用真实插值 t 扫描，再映射到调度器时刻**。FLUX scheduler 有分辨率相关的 mu-shift：t_sched = μt/(1+(μ−1)t)，μ(512²)≈0.5 量级（按所用 scheduler 实现读取）。所有结果图横轴画真实 t |

## 1.3 逐步协议

### S1：有效谱 S_eff(ω) 估计（第 1–2 天）

问题：FLUX latent 为 16 通道、非平稳，S(ω) 无唯一定义。采用**主估计 + 两个稳健性估计**的三轨方案：

1. **主估计（通道平均径向谱）**：
   - 取 1024 张真实图（LAION-aesthetics 子集或 COCO，与模型训练分布接近）+ 1024 张模型自生成图；
   - VAE encode → latent z（应用 scaling 后）；逐通道去均值（去 DC）；
   - 每通道 2D FFT（fp32）→ 功率谱 |ẑ_c(ω)|² → 对径向频率 r = |ω| 做 24 个对数等距 bin 平均 → 得逐通道 S_c(r)；
   - 对 16 通道与全部图片取均值 → S_eff(r)。同时记录通道间离散度（若逐通道谱形状差异大，判决时改用逐通道分析）。
2. **非平稳性处理**：latent 不满足全局平移不变，全局 FFT 会混入边缘效应。加做 **Welch 加窗谱**：32×32 tile、50% 重叠、Hann 窗，逐 tile 求谱后平均。若 Welch 与全局 FFT 在任何 bin 上偏差 > 20%，以 Welch 为准并在报告中注明窗宽修正。
3. **跨通道相关性检查**：计算 16 通道在 latents 上的互相关矩阵；若显著非对角（平均 |ρ| > 0.3），加做 PCA 白化基下的谱作为第三估计。
4. **像素域对照谱**：把 ẑ₀ 解码回像素域测谱 S_pix(r)。**注意**：理论中的 S 是"模型所学数据分布"在扰动所在表示域的谱——扰动加在 latent 速度场上，主分析用 latent 谱；像素谱仅用于解释感知度量。
5. 对 log S_eff ~ log r 在中频段（bin 4–18）拟合幂律得 β̂；报告 β̂ ± 95% CI。真实图与生成图两组谱需一致（bin 级相对差 < 15%），不一致时以生成图谱为准（那才是模型实际学到的分布）。

### S2：脉冲扰动的定义与幅度标定（第 2–3 天）

**扰动形状**（在 latent 空间、逐通道、对 v_θ 的二维空间网格做）：

```
伪代码：
v̂ = FFT2(v_θ)                       # fp32, 逐通道
mask_k = HannTaperRing(r_k, Δr)     # 以 r_k 为中心的窄带环，Δr = 1 个对数 bin
Φ = random_phase(mask_k)            # 每个 (通道, 种子) 独立均匀相位，强制 Hermite 对称
v̂' = v̂ + δ · mask_k · exp(iΦ)
v' = IFFT2(v̂').real
```

- **窄带环而非单频点**：单频点能量为零测度，且非线性会把它涂抹开；环带宽取 1 个对数 bin、Hann  taper。另设 Gabor 型（频带 × 空间局部化窗，窗宽 16×16 latent pixel）变体做非平稳稳健性复核（10 单元 × 8 种子）。
- **幅度标定（信噪可比的关键）**：δ 不按绝对值，而按**带内相对能量**设定：
  δ² · N_band = α² · Σ_{ω∈band} |v̂_θ(ω)|²
  即注入能量 = α² × 该 (t, band) 处速度场自身带内能量。主值 α=0.5；线性度检查扫 α ∈ {0.25, 0.5, 1.0}（见 S4-R3）。
- v_θ 带内能量先在 32 个未扰动轨迹上预统计（每 (t, band) 一张表），全实验共用。

### S3：扰动注入位置（第 3 天）

- 在采样循环中、transformer 前向之后、Euler 更新之前注入：`v ← v + δ(t, ω)`。
- FLUX 的 latent 是 pack 成 2×2 patch 序列的：**先 unpack 回 [16, 64, 64] 二维网格 → FFT 加扰 → IFFT → 重新 pack**，严禁在 patch 序列上直接 FFT（那会测到错误的频率网格）。
- 每个 run 只在目标 t 对应的**单步**注入（脉冲）；另设"持续窗"变体：在 [t−0.025, t+0.025] 内每步注入（积分类扰动，作为 H2b 鉴别工具）。
- SD2.1 侧：模型输出为 v̂（v-prediction），直接在 v̂ 上按同样协议注入；其 latent 4 通道，S_eff 用同一套 S1 流程估计。
- **轨迹缓存**：每个种子先跑一条 clean 轨迹，把 25 个网格 t 处的 x_t 全部缓存（25×16×64×64×2B ≈ 13MB/种子）。扰动 run 从缓存的 x_t 分叉，只需积分 t → 0 段，平均省约一半算力，且严格配对。

### S4：扫描网格（第 4–8 天）

- **t 网格**：25 点 = {0.02, 0.05} ∪ {0.10, 0.15, …, 0.90}（17 点均布）∪ {0.93, 0.95, 0.97, 0.98} 加密噪声端 + {0.075, 0.925}。
- **ω 网格**：10 个对数 bin，覆盖 latent 径向频率从 r=2/64 到 Nyquist。
- **三阶段采样策略**（控制 GPU 总量）：
  - R1 粗扫：25 t × 10 ω × **8 种子** = 2000 run，定位每条带的峰区；
  - R2 加密：对每带预测 t_c ± 0.15 区间加密（t 步长 0.025）+ 全带边缘点，约 60 单元 × **32 种子**；
  - R3 线性度与稳健性：α 三档 × 3 代表带（低/中/高频）× 5 t × 16 种子；Gabor 变体 10 单元 × 8 种子；1024² 抽查 5 单元 × 8 种子；guidance 敏感性 5 t × 4 ω × 8 种子（见 S5）。
- schnell：T=4 步决定 t 只能取 {0.75, 0.50, 0.25}（+端点），只做 4 t × 10 ω × 16 种子的**定性序**检查。
- SD2.1：25 t（映射到 50 个 DDIM 步）× 10 ω × 16 种子。

### S5：CFG / guidance 干扰控制

- FLUX-dev 是 guidance 蒸馏模型，guidance 通过 embedding 张量输入，**不是可分离的 CFG 两项**。协议：主实验**固定 guidance=3.5**（默认工作点）。关闭（=1.0）会改变速度场本身、即改变被测核，属另一个实验条件而非"去混杂"。
- 稳健性子实验：guidance ∈ {1.0, 3.5, 7.0} × 粗网格（5 t × 4 ω × 8 种子），检验峰位对 guidance 的稳定性；若峰位漂移 > 0.1 t，报告中必须分 guidance 给结果。
- SD2.1 是真 CFG：固定 scale=7.5、空负 prompt；不做 scale 扫描（它只是架构普适性对照）。

### S6：终端扰动度量

1. **主度量**：A = ‖Δẑ₀‖₂ / ‖ẑ₀‖₂（latent 域，配对差）。
2. **次度量**：像素域 L2（固定 decoder 后）、LPIPS（感知）。
3. **诊断度量**：Δẑ₀ 的谱分解——扰动是否留在注入带内（带内能量占比）。占比 < 60% 说明强非线性泄漏，作为 H2c 证据记录。
4. 统计：对每种子的 |A| 取均值（相位随机，振幅恒正，不可对复振幅取均值），bootstrap（1000 次）95% CI。

## 1.4 度量与判决（定量）

**GO（定律成立）需同时满足**：
1. ∩ 形：≥7/10 个频带的 A(t) 曲线存在内部极大点（0.05 < t̂_c < 0.95，且峰值 > 两端点均值 ×1.5）；
2. 峰位精度：≥70% 频带 |t̂_c(ω) − S_eff/(1+S_eff)| ≤ 0.10；
3. 定性序：t̂_c 随 ω 单调左移，Spearman ρ ≤ −0.7（p < 0.05）；
4. 幅度压制：在 t=0.9 处，A(ω) 对 log|ω| 的拟合指数 γ 满足 |γ − β̂/2| ≤ 0.3；且最低频带峰值幅度 / 最高频带峰值幅度 ≥ 3；
5. 形状拟合：每带仅用**一个整体竖直缩放系数**拟合理论曲线，log 域 R² ≥ 0.8 的频带占比 ≥ 70%。

**部分成立**：满足 1+3 但 2 或 4 失败 → 换 S 估计量（Welch / 白化 / 像素域）重算一次（仅一次，防多重比较）；仍失败则结论为"定性定律成立、谱标定待修正"。

**NO-GO**：条件 1 失败（无 ∩ 形）→ 进入 H2b/H2c 鉴别（见 1.6）。

**架构普适性判决**：SD2.1 满足上述 1+3 → 定律跨架构成立；不满足 → 结论限定为 RF/MMDiT 特性。schnell 仅要求定性序（条件 3）。

## 1.5 预期产物

- **图 A**：4 条代表频带的 A(t) 实测曲线（带 CI 带）与理论 1/C(t, ω) 叠加，横轴真实 t，双峰对比一目了然；
- **图 B**：log A 的 (t, ω) 热图，叠加理论脊线 t_c(ω) 与实测 argmax（含误差棒）；
- **图 C**：S_eff(r) 三轨估计对比 + β̂ 幂律拟合；
- **图 D**：dev / schnell / SD2.1 归一化曲线对比；
- **表 1**：判决条件 1–5 逐项打勾表；**表 2**：α 线性度（A vs α 的 R²，要求 ≥0.98）；**表 3**：带内能量占比（泄漏诊断）。

## 1.6 风险与退路

| 风险 | 应对 |
|---|---|
| ∩ 形不出现，曲线单调 | 鉴别 H2b：比较脉冲 vs 持续窗扰动；若持续窗出现 ∩ 而脉冲没有，说明理论少了一个积分核因子，修正模型后重判——这本身是有价值的负结果 |
| 响应非线性（表 2 R² < 0.98） | 降 α 至 0.1 重测线性 regime；若 0.1 仍非线性，记录临界 α*，结论转为"线性化灵敏度理论仅适用于 α < α*" |
| S_eff 标定失败（峰位系统性偏移） | 依次试：Welch 谱 → 白化谱 → 像素域谱 → 自由参数 S̃(ω)=c·r^(−β̃) 双参数拟合（此时条件 2 改为拟合优度判决） |
| schnell 4 步分辨率不足 | 只声明定性序；或补一个 schnell-8 步自定义调度作辅助 |
| 任何失败 | 图 B 的完整 (t, ω) 灵敏度地图本身就是此前文献没有的实测产物，无论定律成立与否都可作为独立结果发布 |

## 1.7 GPU 预算与日程

| 项 | 步数估算 | 时耗（A6000 级，512²，~2s/步） |
|---|---|---|
| S1 谱估计（2048 图 encode + 少量采样） | — | ~2 h |
| 带内能量预统计（32 轨迹 × 28 步） | 896 步 | ~0.5 h |
| R1 粗扫（2000 run × 平均 14 步） | 28k 步 | ~16 h |
| R2 加密（1920 run × 平均 8 步） | 15k 步 | ~9 h |
| R3 线性度/稳健性/抽查 | ~12k 步 | ~7 h |
| schnell 全网格（4000 run × 平均 2 步） | 8k 步 | ~2 h |
| SD2.1（4000 run × 平均 12 步） | 48k 步 | ~4 h（UNet 小，~0.15s/步） |
| **合计** | | **≈ 40–45 GPU·h**，单卡约 3–4 天连续跑 |

日程：D1–2 谱估计；D2–3 扰动模块与标定表；D4–6 R1+R2；D7 R3 与对照组；D8–9 分析出图；D10 判决报告。余量 4 天应对返工。

---

# 实验 2：风格因子正交性探针

## 2.1 目标与假设

**主假设 H1**：FLUX 的 adaLN 调制空间中，风格因子（笔触 B、色彩 C、纹理 T）对应近似正交的可识别方向，且该正交性显著强于注意力/激活空间。

**备择假设**：H2a 部分正交（某两因子正交、第三对纠缠）；H2b 方向存在但纠缠可正交化修复；H2c 全面纠缠（NO-GO，但仍产出"纠缠地图"）。

## 2.2 受控数据集构造（第 1–3 天）

### D-A：程序化合成集（主数据集，有因子 GT）
- 用参数化绘制器（程序化 painterly 渲染，如基于笔刷描边的 stroke-based renderer + 可调色板 + 可调颗粒噪声）生成图像，**三个因子各自独立参数化**：
  - 笔触 B：笔触长度/宽度/方向分布（von Mises 集中度 κ）、覆盖密度 → 8 档；
  - 色彩 C：调色板在 Lab 空间的 hue 均值/饱和度/明度分布 → 8 档；
  - 纹理 T：叠加颗粒的功率谱斜率与幅度、周期性 → 8 档。
- **单因子变化设计**：基准配置 × 每次只变一个因子过 8 档，其余冻结 → 3 × 8 × 200 张 = **4800 张**。这给出配对对比（仅因子 f 不同的样本对），是后续差分方向识别的关键。
- QC：用独立属性回归器（见 2.4-C）回测每张图的三个因子值，与设计值相关系数 ≥ 0.95，否则修渲染器。

### D-B：LoRA 已知风格集（半受控）
- 选 3 个风格上近乎单因子的图集各 30–50 张（厚涂 impasto≈笔触、平涂马卡龙色≈色彩、交叉排线 crosshatch≈纹理），各训一个 rank-16 LoRA（lr 1e-4，~800 步，约 1.5–2 h/个）。
- 用 scale λ ∈ {0, 0.5, 1.0} 滴定注入强度，得到"已知因子方向被激活"的样本；LoRA 权重 ΔW 本身也提供一个权重空间的候选因子方向作参照。

### D-C：真实图集（外部效度，不作判决依据）
- WikiArt 子集：10 个流派 × 200 张。因子无 GT，用：(a) 属性回归器预测值；(b) 300 张人工标注子集（3 标注员，ICC > 0.7 才采用）。仅用于检验方向在真实分布上是否仍有语义。

## 2.3 探针方法

### A. 候选特征空间清单（FLUX.1-dev，19 double + 38 single blocks）
1. **adaLN 调制参数（主空间）**：每 block 的 scale/shift（img 分支；double block 另记 txt 分支）。逐 block 分析 + 全 block 拼接（PCA 降至 512 维）两种粒度。
2. **联合注意力 K/V 统计**：FLUX 无独立 cross-attention；取 double block 内 img token 的 K、V 的通道均值/方差/V 的 Gram 统计。**注意**：经典 cross-attn 空间对照只能由 SD2.1 提供；FLUX 内用 txt 分支调制与 txt→img 注意力熵作类比。
3. **调制后激活**：adaLN scale/shift 之后、attn 之前的激活，token 维平均池化。
4. **SD2.1 对照空间**：cross-attn K/V 映射输出、ResBlock 的 GroupNorm 后激活——用于"跨空间正交性对照"的架构外证。

- 提取协议：每图在 t ∈ {0.9, 0.7, 0.5, 0.3, 0.1} 各做 1 次前向（共 5 次），prompt 用中性句"an artwork"；特征按 t 分层保存（正交性可能只在中段 t 成立，需出 block × t 热图）。4800 图 × 5 次前向 ≈ 7–13 GPU·h。

### B. 方向识别（每个特征空间 × 每个因子 × 每个 t 分层）
1. **均值差方向（主）**：d_f = normalize(μ(f=high) − μ(f=low))，仅用配对样本（其余两因子同档），自动抵消 nuisance；
2. **线性探针**：L2 logistic 回归，5 折 CV；探针权重方向作为 d_f 的替代估计；**空间可读的准入门槛：CV 准确率 ≥ 85%**（D-A 集），不达标的空间不参与正交性判决；
3. **PCA**：对配对差分向量集合做 PCA，检查是否一维（解释方差 > 70% 视为点方向，否则升级为子空间假设）；
4. **CCA/PLS**：特征矩阵 × 三因子设计矩阵，提取每因子的多维子空间，给主角度（principal angles）作为余弦的推广。

### C. 正交性度量与显著性
- **余弦矩阵**：C_fg = |⟨d̂_f, d̂_g⟩|，报 off-diag 均值与最大值；
- **基线对照三重**：
  (a) 随机方向基线：高维中随机 |cos| ≈ 1/√d_eff，d_eff 用特征谱的参与比（participation ratio）估计，**不能用原始维度**（特征强相关会低估基线）；
  (b) 标签打乱探针：100 次置换，给 p 值；
  (c) 跨空间对照：调制空间 vs 注意力统计空间 vs 激活空间，配对 bootstrap（以图为重采样单位，1000 次）检验差异；
- **子空间版本**：若 PCA 显示多维，用主角度正弦均值替代余弦。

### D. 因果验证（干预实验，第 7–8 天）
- **方向干预**：推理时把 α·d̂_f 加到对应空间的调制/激活上（逐 block 或仅在 cos 热图显示最正交的 block 段），α ∈ {−2, −1, −0.5, 0.5, 1, 2}（单位：该分量标准差），每 (f, α) 生成 64 张（8 prompt × 8 种子）。
- **"只变目标因子"的判定指标**：训练三个**冻结属性回归器**（在 D-A 上训练，结构与特征空间无关，输入为像素图）：
  - 色彩：Lab 直方图 + 主色 hue 圆均值回归；
  - 笔触：梯度方向直方图集中度 + 笔触长度统计（连通域分析）；
  - 纹理：小波/Gabor 能量谱斜率回归器；
  - 三个回归器在 held-out 上的 R² 均需 ≥ 0.9 才启用。
- **成功判据**：目标属性随 α 单调（Spearman ≥ 0.9），且非目标属性的归一化变化量 ≤ 目标变化量的 20%（**效应比 ≥ 5:1**）；同时 CLIP-IQA / FID（对 D-A 分布）退化 < 10%，排除"靠毁图改属性"。
- **交换测试**：从图 A 提取方向注入图 B 的生成，检验目标因子是否"移植"成功（回归器读数向 A 的因子值移动）。
- **人评子集**：200 对图，5 名标注者强迫选择"哪一属性变了"，一致率 ≥ 80%。

## 2.4 判决标准

**GO（需同时满足）**：
1. adaLN 调制空间三因子探针 CV 准确率均 ≥ 85%；
2. 调制空间 off-diag 平均 |cos| ≤ 0.3，且（a）显著低于随机有效维基线 1/√d_eff 的 95% 上界、（b）配对 bootstrap 显著低于注意力/激活空间（p < 0.01）；
3. 因果验证 ≥ 2/3 因子对满足效应比 ≥ 5:1。

**部分成立降级路径**：
- 恰一对纠缠（如 C–T 的 |cos| > 0.5）：先查数据端——D-A 中该两因子的属性回归器读数是否相关（> 0.3 说明渲染器没解耦，回去修数据再判）；数据干净则结论降为"双因子正交 + 第三对纠缠"，并尝试 Gram–Schmidt 残差方向 + 分 block / 分 t 定位纠缠发生处（出 block × t 的 |cos| 热图，可能只在某些层/时段纠缠）；
- 方向存在但 α 干预多因子联动：尝试干预位置从调制空间移到 PCA 残差子空间，或合并 LoRA 的 ΔW 方向构造混合方向。

**NO-GO**：所有空间 off-diag > 0.5 或因果干预全部多因子 → 假设拒绝；此时"哪个空间纠缠哪对因子"的地图 + LoRA 参照分析仍构成完整的负结果报告。

## 2.5 预期产物

- 图 E：三特征空间 × 三 t 层的余弦矩阵热图组（含随机基线虚线）；
- 图 F：block × t 的 |cos| 热图（纠缠定位图）；
- 图 G：因果干预网格图（3 方向 × 6 α 的生成样张）+ 属性回归器读数曲线（目标单调上升、非目标平坦）；
- 表 4：判决条件逐项表；表 5：探针准确率总表；表 6：人评一致率。

## 2.6 GPU 预算与时间表

| 阶段 | 内容 | 时耗 |
|---|---|---|
| D1–2 | D-A 渲染 + QC 回归器训练 | ~3 h（渲染 CPU 为主） |
| D3 | 3 个 LoRA 训练 + WikiArt 整理 | ~6 h |
| D4–5 | 特征提取（4800×5 前向 + SD2.1 对照 2000×5） | ~13 h |
| D6 | 探针 + 方向 + 正交性统计 + 置换检验（CPU 重） | ~1 h GPU |
| D7–8 | 因果干预生成（3 方向 × 6 α × 64 图 × 28 步 ≈ 32k 步） | ~15 h |
| D9 | 属性回归评估 + 人评 + 稳健性 | ~2 h |
| D10 | 出图、判决报告 | — |
| **合计** | | **≈ 40 GPU·h**，单卡 10 天内可完成（含 CPU 日） |

---

## 附：两实验的全局风险

1. **T5 文本编码器加载**：FLUX 需 T5-XXL（~9GB bf16 或量化版），feature 提取阶段可用预计算 prompt embedding 缓存，把 T5 从显存中卸载，给 transformer 腾空间。
2. **结果可比性**：两实验共用同一批 prompt 池与种子池，便于交叉引用（例如实验 2 的方向干预轨迹可顺带喂给实验 1 的谱分析框架，作为免费的扩展数据）。
3. **最低保障产出**：实验 1 的 (t, ω) 灵敏度地图、实验 2 的纠缠定位热图，在任何判决结果下都是可发表的实测数据，确保实验不存在"白做"的结局。
